# 2026-08-16

## 本次会话完成

1. **目录整理**：源码移入 `code/`，新建 `doc/`（代码与文档分离）
   - `code/KillSounds.csproj` 相对引用路径更新（Harmony/Unity 引用、CopyToRimWorld 目标）
2. **源码文件去前缀重命名**（类同步更名 + 引用更新）
   - KillSoundPicker→SoundPicker、KillSoundsDefOf→DefOf、KillSoundSettings→Settings、KillSoundsMod→Mod、KillStreakTracker→StreakTracker
   - 注意点：`[DefOf]` 特性改为 `[RimWorld.DefOf]` 避免遮蔽；Mod 基类用 `: Verse.Mod`
3. **文档初始化**：`doc/README.md`（总览）、`doc/design.md`（扩展设计：触发条件/事件类型/视觉反馈/设置界面）
4. **视觉反馈基础版**：新增 `code/VisualFeedback.cs`（连杀浮动文字 + 震屏）；设置项 + 中英翻译 key；构建通过（0 警告 0 错误）
5. **Agent 团队搭建**：`opencode.json` + `AGENTS.md` + `.opencode/agents/`×6 + 记忆系统（MEMORY.md + 本日志）+ `doc/CODE_STANDARDS.md`；`doc/AGENTS_CONFIG.md` 移至 `.opencode/docs/`

## 待办（已批准未实现）

- 移除震屏（Settings/VisualFeedback/翻译）
- 浮动文字从死亡位置改为击杀者头顶（`killer.DrawPos + Vector3.up * 1.5f`，保留两级开关）

## 核心设计要点

- 新管线：Postfix → 触发判定 → StreakTracker → SoundPicker → 播放 → VisualFeedback
- 视觉反馈取死亡位置需用 `victim.Corpse`（`Pawn.Kill` 执行中 pawn 会被 despawn）
